# CIS CLI AI-Native 重构计划

## 背景与目标

### 当前问题

1. **向量引擎**: init 时仅配置，未实际下载绑定
2. **CLI 设计**: 面向人类设计，AI 调用困难
3. **目录混乱**: 安装路径分散，难以查找和清理
4. **GUI 交互**: 不符合人类操作习惯

### 目标

打造 **AI-Native** 的 CIS  CLI，同时优化人类 GUI 体验。

---

## 1. 向量引擎自动下载绑定

### 当前状态
- init 时仅提示配置，不自动下载
- 用户需手动下载模型或使用 API

### 目标状态
- init 时自动检测、下载、绑定向量模型
- 零配置即可使用语义搜索

### 实现方案

```
┌─────────────────────────────────────────────────────────────┐
│                  向量引擎初始化流程                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 检测现有模型                                             │
│     ├── 检查 ~/.cis/models/nomic-embed-text-v1.5/           │
│     └── 如果存在 → 跳过下载                                  │
│                                                              │
│  2. 自动下载（异步）                                         │
│     ├── 显示进度条                                           │
│     ├── 下载 model.onnx (~130MB)                            │
│     ├── 下载 tokenizer.json                                 │
│     └── 验证文件完整性                                       │
│                                                              │
│  3. 绑定配置                                                 │
│     ├── 写入 ~/.cis/config.toml                             │
│     ├── 设置 embedding_dim = 768                            │
│     └── 测试向量引擎可用性                                   │
│                                                              │
│  4. 失败回退                                                 │
│     ├── 下载失败 → 提示使用 API 模式                        │
│     └── 网络不通 → 使用 SQL 回退                            │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 关键改动

**文件**: `cis-core/src/init/wizard.rs`

```rust
async fn download_and_bind_vector_engine(&self) -> Result<()> {
    let model_dir = Paths::models_dir().join("nomic-embed-text-v1.5");
    
    // 1. 检查是否已存在
    if model_exists(&model_dir) {
        println!("✓ 向量模型已存在");
        return Ok(());
    }
    
    // 2. 创建目录
    std::fs::create_dir_all(&model_dir)?;
    
    // 3. 异步下载
    let pb = ProgressBar::new(130_000_000); // 130MB
    download_with_progress(
        "https://huggingface.co/nomic-ai/nomic-embed-text-v1.5/...",
        &model_dir.join("model.onnx"),
        pb
    ).await?;
    
    // 4. 绑定配置
    bind_vector_config(&model_dir).await?;
    
    // 5. 测试
    test_vector_engine().await?;
    
    Ok(())
}
```

---

## 2. CLI 面向 AI 设计

### 设计原则

| 原则 | 说明 | 示例 |
|------|------|------|
| **结构化输出** | 默认 JSON，便于 AI 解析 | `cis status --json` |
| **机器可读** | 状态码明确，错误信息标准化 | 退出码 0=成功，1=失败，2=需确认 |
| **原子操作** | 命令幂等，可重复执行 | `cis init` 可安全重试 |
| **上下文感知** | 自动检测环境，减少参数 | 自动发现 `.cis/` 目录 |
| **批量操作** | 支持列表输入和并行 | `cis task run --batch tasks.json` |

### 命令重构

#### 当前命令（人类友好）
```bash
cis skill do "修复 bug"
cis memory search "上次讨论的架构"
```

#### AI 友好版本
```bash
# 结构化输入/输出
cis skill invoke --name "code-fix" --input '{"issue": "bug"}' --json
cis memory query --text "架构讨论" --format json --limit 10

# 批量操作
cis dag run --definition workflow.json --json
cis task status --run-id xyz --watch --json

# 环境检测
cis env check --json  # 输出环境状态，AI 可据此决定下一步
```

### 退出码规范

| 退出码 | 含义 | AI 处理建议 |
|--------|------|-------------|
| 0 | 成功 | 继续下一步 |
| 1 | 一般错误 | 查看 stderr，重试或报告 |
| 2 | 需要确认 | 调用 `--force` 或请求用户 |
| 3 | 配置缺失 | 运行 `cis init` |
| 4 | 网络错误 | 等待后重试 |
| 5 | 权限错误 | 请求提升权限 |

### AI 专用命令

```bash
# AI 代理模式（单命令完成复杂任务）
cis agent complete --task "修复所有警告" --context ./src --json

# 自动修复模式
cis fix auto --scope project --dry-run --json

# 智能建议
cis suggest --for "优化性能" --based-on logs/ --json
```

---

## 3. 目录整合

### 当前目录结构（混乱）
```
~/.cis/                    # 配置
~/.cis/data/               # 数据
~/.cis/models/             # 模型
/tmp/cis-target/           # 编译缓存（Cargo 设置）
/var/log/cis/              # 日志（如果 systemd）
./.cis/                    # 项目配置
```

### 整合后目录结构
```
~/.local/share/cis/        # 数据（遵循 XDG）
├── bin/                   # 二进制（可选）
├── config/                # 配置文件
│   ├── config.toml
│   ├── embedding.toml
│   └── keys/
├── data/                  # 运行时数据
│   ├── memory.db
│   ├── vector.idx
│   └── sessions/
├── models/                # AI 模型
│   └── nomic-embed-text-v1.5/
├── logs/                  # 日志
│   └── cis-node.log
└── cache/                 # 缓存
    ├── downloads/
    └── tmp/

~/.config/cis/             # XDG 配置目录（软链到上面）
~/.cache/cis/              # XDG 缓存目录（软链到上面）
```

### 迁移策略

```rust
pub struct UnifiedPaths;

impl UnifiedPaths {
    /// 获取统一的基础目录
    pub fn base_dir() -> PathBuf {
        // 1. 优先使用 XDG 标准
        if let Ok(data_home) = env::var("XDG_DATA_HOME") {
            return PathBuf::from(data_home).join("cis");
        }
        
        // 2. 回退到 ~/.local/share/cis
        dirs::data_dir().unwrap().join("cis")
    }
    
    /// 迁移旧配置
    pub fn migrate_legacy() -> Result<()> {
        let old_config = dirs::home_dir().unwrap().join(".cis");
        let new_config = Self::base_dir();
        
        if old_config.exists() && !new_config.exists() {
            println!("Migrating legacy config...");
            std::fs::rename(&old_config, &new_config)?;
        }
        
        Ok(())
    }
}
```

### 清理卸载

```bash
# 一键清理所有数据
cis system uninstall --purge

# 仅清理缓存
cis system clean --cache

# 仅清理日志
cis system clean --logs

# 导出数据后清理
cis system export --all --to ./backup.tar.gz
cis system uninstall --purge
```

---

## 4. GUI 模仿 Element 重构

### Element 交互特点

1. **侧边栏导航**: 固定左侧，快速切换功能
2. **会话列表**: 中间列显示会话/房间
3. **内容区域**: 右侧显示详情
4. **全局搜索**: 顶部搜索栏，快速查找
5. **快捷操作**: 右键菜单，键盘快捷键

### GUI 布局设计

```
┌─────────────────────────────────────────────────────────────┐
│  CIS GUI (Element-style)                                    │
├──────────┬──────────────┬───────────────────────────────────┤
│          │              │                                   │
│  Sidebar │  Session     │  Content Area                     │
│          │  List        │                                   │
│  ┌────┐  │  ┌────────┐  │  ┌─────────────────────────────┐  │
│  │ 🏠 │  │  │ DAG-1  │  │  │  DAG 执行可视化             │  │
│  │ 💬 │  │  │ DAG-2  │  │  │  ├── Task A ✅              │  │
│  │ 📊 │  │  │ DAG-3  │  │  │  ├── Task B 🔄              │  │
│  │ 🔧 │  │  └────────┘  │  │  └── Task C ⏳              │  │
│  └────┘  │              │  └─────────────────────────────┘  │
│  Home    │  Quick       │                                   │
│  Chat    │  Switcher    │  ┌─────────────────────────────┐  │
│  Stats   │              │  │  Details / Logs             │  │
│  Settings│              │  │  [Output] [Logs] [Context]  │  │
│          │              │  └─────────────────────────────┘  │
├──────────┴──────────────┴───────────────────────────────────┤
│  Global Search: [________________]  [+] New DAG  [⚙] Settings│
└─────────────────────────────────────────────────────────────┘
```

### 视图设计

#### 1. Home 视图（仪表盘）
- 最近 DAG 运行
- 快速操作按钮
- 系统状态概览

#### 2. DAG 视图（类似 Element 的房间）
- DAG 列表（中间列）
- DAG 详情和可视化（右侧）
- 实时状态更新

#### 3. Chat 视图（AI 对话）
- 与 Agent 的交互历史
- 自然语言输入
- 代码片段高亮

#### 4. Settings 视图
- 配置编辑器
- 插件管理
- 主题切换

### 交互细节

```rust
// 快捷操作示例
pub struct KeyboardShortcuts {
    // 全局
    pub search: KeyCombo::CtrlSlash,
    pub new_dag: KeyCombo::CtrlN,
    pub settings: KeyCombo::CtrlComma,
    
    // DAG 视图
    pub run_dag: KeyCombo::CtrlEnter,
    pub pause_dag: KeyCombo::Space,
    pub attach_session: KeyCombo::CtrlA,
    
    // 导航
    pub goto_home: KeyCombo::Alt1,
    pub goto_dags: KeyCombo::Alt2,
    pub goto_chat: KeyCombo::Alt3,
}
```

---

## 5. 实施计划

### Phase 1: 目录整合（Week 1）
- [ ] 实现 `UnifiedPaths` 模块
- [ ] 添加配置迁移逻辑
- [ ] 更新所有路径引用
- [ ] 测试卸载/清理功能

### Phase 2: 向量引擎自动绑定（Week 1-2）
- [ ] 实现异步下载（带进度条）
- [ ] 添加模型校验
- [ ] 集成到 init 向导
- [ ] 失败回退机制

### Phase 3: CLI AI-Native（Week 2-3）
- [ ] 添加 `--json` 输出到所有命令
- [ ] 实现标准化退出码
- [ ] 添加 `cis agent` 系列命令
- [ ] 更新文档和示例

### Phase 4: GUI Element 风格（Week 3-4）
- [ ] 重构 GUI 布局框架
- [ ] 实现三栏布局
- [ ] 添加快捷键系统
- [ ] 主题和样式调整

### Phase 5: 整合测试（Week 4）
- [ ] 端到端测试
- [ ] 性能测试
- [ ] 文档完善
- [ ] 发布准备

---

## 6. 影响评估

### 破坏性变更
- 目录结构变更（自动迁移）
- CLI 输出格式变更（添加 `--json` 是新增，不破坏）
- GUI 界面完全重构

### 兼容性
- 配置文件格式保持不变
- API 兼容性保持
- 数据库兼容性保持

### 回滚策略
- 保留旧配置备份
- 提供 `cis system rollback` 命令
- 版本标签管理

---

## 7. 成功标准

- [ ] `cis init` 后向量引擎立即可用
- [ ] AI 可以通过 CLI 完成复杂任务
- [ ] 用户可以在 30 秒内找到任何数据
- [ ] GUI 操作符合 Element 用户直觉
- [ ] 卸载后无残留文件

---

## 附录

### 参考链接
- Element Desktop: https://github.com/vector-im/element-desktop
- XDG Base Directory: https://specifications.freedesktop.org/basedir-spec/basedir-spec-latest.html
- CLI Design Guidelines: https://clig.dev/

### 相关文档
- `deploy/docs/VECTOR_ENGINE_INIT_IMPROVEMENT.md`
- `deploy/docs/DAG_MIGRATION_GUIDE.md`
