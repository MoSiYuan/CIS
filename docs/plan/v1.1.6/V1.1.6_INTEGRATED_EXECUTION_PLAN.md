# CIS v1.1.6 整合执行计划

> **整合日期**: 2026-02-12
> **版本**: v1.1.6
> **状态**: Phase 1-3 完成，Phase 4-5 部分完成
> **目标**: 整合所有设计文档，拆分任务，并行开发

---

## 📋 执行摘要

### Phase 1-3 完成情况

**已完成**: 13/13 Teams (100%)
- Teams A-L: 核心架构和功能开发
- Team M: CLI 重构（部分完成，需要调整）
- Teams N-O: Matrix/MCP 协议（已完成）

**交付成果**:
- ~21,000 行新代码
- ~10,000 行测试
- 9 个审阅报告
- 完整的 Phase 1-3 文档

### Phase 4-5 完成情况

**已完成**: 4/4 Teams (100%)
- Team M: CLI 重构（已完成但需调整架构）
- Team N: Matrix 协议（已完成）
- Team O: MCP 协议（已完成）
- Team P: CLI 命令 + 倒计时（已完成）

**交付成果**:
- ~8,470 行新代码
- ~4,000 行测试
- Phase 4-5 进度报告

### 新增架构设计文档

**新增**: 6 个核心设计文档
1. MONOLITHIC_MODULES_ANALYSIS.md - 17 个巨型模块分析
2. CODE_REUSE_STRUCTURE_DESIGN.md - 代码复用结构设计
3. CLI_GUIDE_OPTIMIZED.md - CLI 使用指南优化
4. REFACTORING_EXECUTION_PLAN.md - 8 周重构计划
5. MEMORY_ARCHITECTURE_DESIGN.md - 记忆架构设计
6. MEMORY_INDEX_PRECISION_DESIGN.md - 精准索引设计

---

## 🎯 核心问题识别

### 1. 架构层面

#### 问题 A: CLI 架构偏差
**发现**: Team M 的 CLI handler 直接实现业务逻辑
```rust
// ❌ 错误模式
fn init_cis() {
    use std::fs;
    fs::create_dir_all(&cis_dir)?;  // 直接操作文件系统
    generate_project_config();
    fs::write(&config_file, config)?;
}
```

**影响**:
- CLI/GUI/远程 API 无法复用逻辑
- 违反"CLI 基于 Server API"的原则
- 需要重构

**解决方案**: 参见 CLI_GUIDE_OPTIMIZED.md

---

#### 问题 B: 巨型模块过多
**发现**: 17 个模块 >600 行，最大 3,439 行

**Top 5 巨型模块**:
1. scheduler/mod.rs - 3,439 行（DAG 调度系统）
2. config/loader.rs - 1,034 行（配置加载）
3. skill/chain.rs - 1,390 行（Skill 链）
4. skill/router.rs - 1,287 行（Skill 路由）
5. skill/manager.rs - 1,038 行（Skill 管理）

**影响**:
- 可维护性差（单一职责违反）
- 编译缓慢（增量编译 ~120s）
- 测试困难（紧耦合）

**解决方案**: 参见 REFACTORING_EXECUTION_PLAN.md

---

#### 问题 C: 记忆系统全量向量索引
**发现**: 所有记忆都向量化，导致检索失真

**问题表现**:
- 向量数据库持续增长（最终数万条）
- 语义搜索返回模糊匹配
- 内存占用巨大（HNSW 需要 ~200MB）
- 无自动归档机制

**解决方案**: 参见 MEMORY_INDEX_PRECISION_DESIGN.md
- 精准向量索引（只索引 ~10%）
- 54 周按周分 db 归档
- 索引指向日志 ID（引用模式）

---

### 2. 执行层面

#### 问题 D: 缺少统一协调
**发现**: Teams 独立开发，缺少统一协调

**影响**:
- 接口不一致
- 代码风格差异
- 重复造轮子

**解决方案**: Agent Teams 模式（见下文）

---

## 🚀 Agent Teams 并行开发模式

### 核心理念

**传统模式**（已废弃）:
```
用户 ─▶ Claude ─▶ 创建任务 ─▶ 人工分配 ─▶ Team 执行 ─▶ 报告
                                      ↑
                                   缓慢、手动
```

**Agent Teams 模式**（新）:
```
用户 ─▶ Claude ─▶ AgentPool ─▶ 自动创建 Teams ─▶ 并发执行 ─▶ 自动协调
              ↓              ↓               ↓
           任务拆分        自管理          事件同步
```

### AgentPool 架构

```rust
use cis_core::agent::pool::{AgentPool, TeamSpec, TaskSpec};

/// Agent Pool（统一管理所有 Agent Teams）
pub struct AgentPool {
    /// 活跃的 Teams
    active_teams: HashMap<TeamId, AgentTeam>,

    /// 任务队列
    task_queue: VecDeque<TaskSpec>,

    /// 事件总线（Teams 间通信）
    event_bus: Arc<EventBus>,
}

impl AgentPool {
    /// 创建 Agent Pool
    pub fn new() -> Self {
        Self {
            active_teams: HashMap::new(),
            task_queue: VecDeque::new(),
            event_bus: Arc::new(EventBus::new(1000)),
        }
    }

    /// 启动并行开发（一键启动）
    pub async fn start_parallel_dev(
        &mut self,
        teams: Vec<TeamSpec>,
        tasks: Vec<TaskSpec>,
    ) -> Result<ParallelReport> {
        // 1. 创建 Teams（并发）
        for team_spec in teams {
            let team = AgentTeam::create(team_spec, self.event_bus.clone()).await?;
            self.active_teams.insert(team.id.clone(), team);
        }

        // 2. 分配任务（自动负载均衡）
        for task in tasks {
            self.assign_task(task).await?;
        }

        // 3. 启动所有 Teams（并发）
        let futures: Vec<_> = self.active_teams.values_mut()
            .map(|team| team.start())
            .collect();

        let results = futures::future::join_all(futures).await;

        // 4. 收集报告
        Ok(ParallelReport::from_results(results))
    }

    /// 分配任务（智能调度）
    async fn assign_task(&mut self, task: TaskSpec) -> Result<()> {
        // 1. 分析任务依赖
        let deps = self.resolve_dependencies(&task).await?;

        // 2. 找到合适的 Team
        let best_team = self.find_best_team(&task, &deps)?;

        // 3. 分配任务
        best_team.assign_task(task).await?;

        // 4. 发布事件（通知其他 Teams）
        self.event_bus.publish(TaskAssignedEvent {
            task_id: task.id.clone(),
            team_id: best_team.id.clone(),
        }).await?;

        Ok(())
    }

    /// 查找最佳 Team（基于负载和能力）
    fn find_best_team(
        &self,
        task: &TaskSpec,
        deps: &[TaskId],
    ) -> Result<&AgentTeam> {
        // 1. 筛选有能力的 Teams
        let capable: Vec<_> = self.active_teams.values()
            .filter(|t| t.can_handle(task))
            .collect();

        // 2. 筛选依赖满足的 Teams
        let ready: Vec<_> = capable.into_iter()
            .filter(|t| deps.iter().all(|d| t.has_completed(*d)))
            .collect();

        // 3. 选择负载最低的
        ready.into_iter()
            .min_by_key(|t| t.task_queue.len())
            .ok_or_else(|| Error::NoAvailableTeam)
    }
}
```

### AgentTeam 定义

```rust
/// Agent Team（自动管理的开发 Team）
pub struct AgentTeam {
    /// Team ID
    pub id: TeamId,

    /// Team 名称
    pub name: String,

    /// Team 能力（能处理的任务类型）
    pub capabilities: Vec<TaskType>,

    /// 当前任务队列
    task_queue: VecDeque<TaskSpec>,

    /// Team Runtime（Claude/OpenCode/Kimi）
    runtime: Box<dyn AgentRuntime>,

    /// 事件订阅
    event_subscriptions: Vec<EventListener>,

    /// 状态
    pub status: TeamStatus,
}

#[derive(Debug, Clone)]
pub enum TeamStatus {
    Created,
    Running,
    Busy,
    Idle,
    Completed,
    Failed(String),
}

impl AgentTeam {
    /// 创建 Team
    pub async fn create(
        spec: TeamSpec,
        event_bus: Arc<EventBus>,
    ) -> Result<Self> {
        // 1. 创建 Runtime
        let runtime = match spec.runtime_type {
            RuntimeType::Claude => ClaudeRuntime::new().await?,
            RuntimeType::OpenCode => OpenCodeRuntime::new().await?,
            RuntimeType::Kimi => KimiRuntime::new().await?,
        };

        // 2. 订阅事件
        let subscriptions = vec![
            event_bus.subscribe("task.assigned").await?,
            event_bus.subscribe("task.completed").await?,
        ];

        Ok(Self {
            id: generate_id(),
            name: spec.name,
            capabilities: spec.capabilities,
            task_queue: VecDeque::new(),
            runtime,
            event_subscriptions: subscriptions,
            status: TeamStatus::Created,
        })
    }

    /// 启动 Team（后台运行）
    pub async fn start(&mut self) -> Result<TeamReport> {
        self.status = TeamStatus::Running;

        loop {
            // 1. 获取下一个任务
            let task = match self.task_queue.pop_front() {
                Some(t) => t,
                None => {
                    // 无任务，等待事件
                    tokio::time::sleep(Duration::from_secs(1)).await;
                    continue;
                }
            };

            // 2. 执行任务
            self.status = TeamStatus::Busy;
            let result = self.execute_task(task).await;

            // 3. 更新状态
            match result {
                Ok(_) => {
                    self.status = TeamStatus::Running;
                    // 发布完成事件
                    // self.event_bus.publish(...).await?;
                }
                Err(e) => {
                    self.status = TeamStatus::Failed(e.to_string());
                    break;
                }
            }
        }

        // 4. 生成报告
        Ok(self.generate_report())
    }

    /// 执行任务（使用 Runtime）
    async fn execute_task(&self, task: TaskSpec) -> Result<TaskResult> {
        // 使用 Runtime 执行
        self.runtime.execute(task).await
    }

    /// 能否处理此任务
    pub fn can_handle(&self, task: &TaskSpec) -> bool {
        self.capabilities.contains(&task.task_type)
    }
}

/// Team 规格
#[derive(Debug, Clone)]
pub struct TeamSpec {
    pub name: String,
    pub runtime_type: RuntimeType,
    pub capabilities: Vec<TaskType>,
    pub max_concurrent: usize,
}
```

### TaskSpec 定义

```rust
/// 任务规格
#[derive(Debug, Clone)]
pub struct TaskSpec {
    pub id: TaskId,
    pub name: String,
    pub task_type: TaskType,
    pub priority: TaskPriority,

    /// 任务描述（给 Agent 的 prompt）
    pub prompt: String,

    /// 依赖的任务 ID
    pub dependencies: Vec<TaskId>,

    /// 预估工作量（人日）
    pub estimated_effort: PersonDays,

    /// 上下文数据（代码、文档等）
    pub context: TaskContext,
}

#[derive(Debug, Clone, PartialEq)]
pub enum TaskType {
    /// 代码审阅
    CodeReview,

    /// 模块重构
    ModuleRefactoring,

    /// 测试编写
    TestWriting,

    /// 文档编写
    Documentation,

    /// 性能优化
    PerformanceOptimization,

    /// 安全加固
    SecurityHardening,
}

#[derive(Debug, Clone, PartialEq, Eq, PartialOrd, Ord)]
pub enum TaskPriority {
    Critical,   // P0
    High,       // P1
    Medium,     // P2
    Low,        // P3
}
```

---

## 📊 任务拆分（基于现有设计）

### 优先级 P0 - 架构修复（Week 1）

#### V-1: CLI 架构修复
**负责**: Team V (CLI 优化团队）
**工作量**: 3-5 人日
**优先级**: P0 (Critical)

**子任务**:
- [ ] 审查所有 CLI handler
  - [ ] cis-node/src/cli/handlers/core/init.rs
  - [ ] cis-node/src/cli/handlers/memory/*.rs
  - [ ] cis-node/src/cli/handlers/project/*.rs
- [ ] 识别直接业务逻辑实现（使用 std::fs, std::process 等）
- [ ] 重构为 Server API 调用
  - [ ] 构建请求对象
  - [ ] 调用 `ctx.server.handle()`
  - [ ] 处理响应状态码
- [ ] 更新 CLI_GUIDE_OPTIMIZED.md
- [ ] 编写单元测试
- [ ] 编写集成测试

**验收标准**:
- 所有 CLI handler 只调用 Server API
- 无直接文件系统操作
- 测试覆盖率 > 80%
- 文档更新完整

**依赖**: 无

---

### 优先级 P1 - 核心重构（Week 2-4）

#### V-2: scheduler 拆分
**负责**: Team Q (核心重构团队)
**工作量**: 10-15 人日
**优先级**: P1 (High)

**子任务**:
- [ ] 设计 scheduler 拆分方案（参考 REFACTORING_EXECUTION_PLAN.md）
  - [ ] scheduler/core/ - DAG 核心 (~300 行)
  - [ ] scheduler/execution/ - 执行器 (~400 行)
  - [ ] scheduler/events/ - 事件系统 (~300 行)
  - [ ] scheduler/persistence/ - 持久化 (~250 行)
  - [ ] scheduler/notification/ - 通知 (~250 行)
- [ ] 实现各子模块
  - [ ] 提取并重构现有代码
  - [ ] 编写单元测试
  - [ ] 编写集成测试
- [ ] 更新 scheduler/mod.rs 导出
- [ ] 性能基准测试
- [ ] 更新文档

**验收标准**:
- scheduler/mod.rs <500 行
- 每个子模块 <500 行
- 测试覆盖率 > 85%
- 性能无回退

**依赖**: V-1 (需要事件总线）

---

#### V-3: config 拆分
**负责**: Team R (配置重构团队)
**工作量**: 3-5 人日
**优先级**: P1 (High)

**子任务**:
- [ ] 拆分 config/loader.rs (1034 行)
  - [ ] loader/file.rs - 文件加载
  - [ ] loader/parser.rs - TOML/YAML 解析
  - [ ] loader/validator.rs - 配置验证
  - [ ] loader/defaults.rs - 默认值
- [ ] 拆分 config/p2p.rs (916 行)
- [ ] 拆分 config/security.rs (648 行)
- [ ] 拆分 config/wasm.rs (636 行)
- [ ] 编写单元测试
- [ ] 更新文档

**验收标准**:
- 每个子模块 <250 行
- 测试覆盖率 > 80%
- 配置加载时间无增加

**依赖**: 无

---

#### V-4: memory 精准索引
**负责**: Team V (记忆优化团队)
**工作量**: 12-15 人日
**优先级**: P1 (High)

**子任务**:
- [ ] 实现 LogMemory 模块（按周分 db）
  - [ ] 当前周数据库管理
  - [ ] 批量写入优化
  - [ ] 访问统计更新
  - [ ] 跨周查询支持
- [ ] 实现 VectorIndex 模块（精准索引）
  - [ ] 索引类型分类
  - [ ] 权重计算
  - [ ] LRU 淘汰策略
  - [ ] HNSW 集成
- [ ] 实现 WeekArchiver 模块（54 周归档）
  - [ ] 周归档任务
  - [ ] 自动清理（54 周滚动）
  - [ ] 压缩支持
- [ ] 数据迁移脚本
  - [ ] 从旧 VectorStorage 迁移
  - [ ] 重建精准索引
- [ ] 编写单元测试
- [ ] 编写集成测试
- [ ] 性能基准测试

**验收标准**:
- 向量索引规模 <10,000 条
- 搜索延迟 <80ms
- 内存占用 <100MB
- 测试覆盖率 > 85%

**依赖**: 无

---

### 优先级 P2 - 次要重构（Week 5-6）

#### V-5: skill 拆分
**负责**: Team T (Skill 重构团队)
**工作量**: 4-6 人日
**优先级**: P2 (Medium)

**子任务**:
- [ ] 拆分 skill/chain.rs (1390 行)
- [ ] 拆分 skill/router.rs (1287 行)
- [ ] 拆分 skill/manager.rs (1038 行)
- [ ] 编写单元测试
- [ ] 更新文档

**验收标准**:
- 每个子模块 <400 行
- 测试覆盖率 > 80%

**依赖**: V-2 (事件总线)

---

#### V-6: p2p 拆分
**负责**: Team S (P2P 重构团队)
**工作量**: 5-7 人日
**优先级**: P2 (Medium)

**子任务**:
- [ ] 拆分 p2p/transport_secure.rs (1536 行)
- [ ] 拆分 p2p/nat.rs (936 行)
- [ ] 拆分 p2p/network.rs (1097 行)
- [ ] 编写单元测试
- [ ] 更新文档

**验收标准**:
- 每个子模块 <500 行
- 测试覆盖率 > 75%

**依赖**: 无

---

#### V-7: agent/其他模块拆分
**负责**: Team U (其他重构团队)
**工作量**: 3-4 人日
**优先级**: P2 (Medium)

**子任务**:
- [ ] 拆分 agent/guard.rs (692 行)
- [ ] 拆分其他大模块
  - [ ] agent/leak_detector.rs (535 行)
  - [ ] agent/config.rs (491 行)
  - [ ] p2p/sync.rs (638 行)
  - [ ] p2p/dht.rs (921 行，或标记废弃)
- [ ] 编写单元测试
- [ ] 更新文档

**验收标准**:
- 每个子模块 <500 行
- 测试覆盖率 > 75%

**依赖**: 无

---

### 优先级 P3 - 验收和优化（Week 7-8）

#### V-8: 全面测试
**负责**: 全员 + QA
**工作量**: 5-8 人日
**优先级**: P3 (Low)

**子任务**:
- [ ] 单元测试补充
  - [ ] 目标覆盖率 >85%
  - [ ] 边界情况测试
  - [ ] 错误路径测试
- [ ] 集成测试
  - [ ] 端到端场景
  - [ ] 性能压力测试
- [ ] CI 配置
  - [ ] 覆盖率门禁
  - [ ] 性能回归检测

**验收标准**:
- 整体覆盖率 >85%
- 所有核心模块 >90%
- CI 门禁生效

**依赖**: V-1 到 V-7 (所有重构任务)

---

#### V-9: 性能优化
**负责**: 性能团队
**工作量**: 2-3 人日
**优先级**: P3 (Low)

**子任务**:
- [ ] 编译时间优化
  - [ ] 目标增量编译 <60s
  - [ ] 使用 `cargo build --timings` 分析
- [ ] 运行时性能优化
  - [ ] 使用 `cargo flamegraph` 分析
  - [ ] 内存占用优化（heaptrack）
- [ ] 性能基准对比
  - [ ] 重构前后对比
  - [ ] 性能回归检测

**验收标准**:
- 增量编译 <60s
- 运行时性能无回退
- 内存占用 <150MB

**依赖**: V-8 (测试完成)

---

#### V-10: 文档更新
**负责**: 技术写作 + 全员
**工作量**: 2-3 人日
**优先级**: P3 (Low)

**子任务**:
- [ ] 更新 CLAUDE.md
  - [ ] 架构说明
  - [ ] API 使用指南
  - [ ] 最佳实践
- [ ] 更新 README.md
- [ ] 编写迁移指南
  - [ ] 从旧 API 迁移到新 API
  - [ ] 数据迁移步骤
- [ ] 编写示例代码

**验收标准**:
- 所有公开 API 有文档
- 示例代码可直接运行
- 文档覆盖率 >90%

**依赖**: V-1 到 V-9 (所有任务完成)

---

## 🚀 AgentPool 并行执行流程

### 启动脚本

```bash
#!/bin/bash
# cis-v1.1.6-parallel-dev.sh

set -e

echo "🚀 启动 CIS v1.1.6 并行开发"

# 1. 创建 Agent Pool
echo "📦 创建 Agent Pool..."
POOL_ID=$(cis agent pool create --name "v1.1.6-refactor")

# 2. 定义 Teams
echo "👥 定义 Agent Teams..."
cis agent pool add-team $POOL_ID \
    --name "Team-V-CLI" \
    --runtime "claude" \
    --capabilities "CodeReview,ModuleRefactoring" \
    --max-concurrent 3

cis agent pool add-team $POOL_ID \
    --name "Team-Q-Core" \
    --runtime "claude" \
    --capabilities "ModuleRefactoring,TestWriting" \
    --max-concurrent 5

cis agent pool add-team $POOL_ID \
    --name "Team-R-Config" \
    --runtime "claude" \
    --capabilities "ModuleRefactoring" \
    --max-concurrent 3

cis agent pool add-team $POOL_ID \
    --name "Team-V-Memory" \
    --runtime "claude" \
    --capabilities "ModuleRefactoring,PerformanceOptimization" \
    --max-concurrent 4

cis agent pool add-team $POOL_ID \
    --name "Team-T-Skill" \
    --runtime "claude" \
    --capabilities "ModuleRefactoring,TestWriting" \
    --max-concurrent 3

cis agent pool add-team $POOL_ID \
    --name "Team-S-P2P" \
    --runtime "claude" \
    --capabilities "ModuleRefactoring" \
    --max-concurrent 3

cis agent pool add-team $POOL_ID \
    --name "Team-U-Other" \
    --runtime "claude" \
    --capabilities "ModuleRefactoring,TestWriting" \
    --max-concurrent 3

# 3. 定义任务（从任务拆分文档）
echo "📋 加载任务..."
cis agent pool load-tasks $POOL_ID \
    --from "docs/plan/v1.1.6/V1.1.6_INTEGRATED_EXECUTION_PLAN.md"

# 4. 启动并行执行
echo "🎯 启动并行开发..."
cis agent pool start-parallel $POOL_ID \
    --max-teams 7 \
    --event-bus-port 7678

echo "✅ Agent Pool 已启动"
echo "查看状态: cis agent pool status $POOL_ID"
echo "查看日志: cis agent pool logs $POOL_ID --follow"
```

### AgentPool CLI 命令

```bash
# Pool 管理
cis agent pool create --name <name>
cis agent pool list
cis agent pool status <pool-id>
cis agent pool delete <pool-id>

# Team 管理
cis agent pool add-team <pool-id> --name <name> --runtime <claude|opencode>
cis agent pool list-teams <pool-id>
cis agent pool team-status <pool-id> <team-id>

# 任务管理
cis agent pool add-task <pool-id> --type <type> --priority <p0|p1|p2|p3>
cis agent pool list-tasks <pool-id>
cis agent pool assign-task <pool-id> <task-id> --team <team-id>

# 并行执行
cis agent pool start-parallel <pool-id> --max-teams <n>
cis agent pool stop-parallel <pool-id>
cis agent pool pause <pool-id>
cis agent pool resume <pool-id>

# 监控
cis agent pool logs <pool-id> --follow
cis agent pool metrics <pool-id>
cis agent pool report <pool-id>
```

---

## 📊 执行时间表

### Week 1: 架构修复

```
Day 1-2: V-1 CLI 架构修复
  Team V: 审查和重构所有 CLI handler

Day 3-5: V-2 scheduler 设计
  Team Q: 设计 scheduler 拆分方案
```

### Week 2-3: 核心重构

```
Week 2:
  Team Q: scheduler 拆分 (10-15 人日)
  Team R: config 拆分 (3-5 人日)

Week 3:
  Team Q: scheduler 拆分 (继续)
  Team V: memory 精准索引 (12-15 人日，启动)
```

### Week 4-5: 次要重构

```
Week 4:
  Team T: skill 拆分 (4-6 人日)
  Team S: p2p 拆分 (5-7 人日)
  Team V: memory 精准索引 (继续)

Week 5:
  Team U: agent/其他 拆分 (3-4 人日)
  Team V: memory 精准索引 (完成)
```

### Week 6: 验收和优化

```
Day 1-3: V-8 全面测试
  全员 + QA: 单元测试、集成测试

Day 4-5: V-9 性能优化
  性能团队: 编译时间、运行时性能

Day 6-7: V-10 文档更新
  技术写作 + 全员: 文档、示例
```

### Week 7-8: 缓冲和发布

```
Week 7:
  缓冲时间：处理意外问题
  回归测试
  性能调优

Week 8:
  最终验收
  发布准备
  v1.1.6 发布
```

---

## 🎯 成功指标

### 代码质量

| 指标 | 当前 | 目标 | 测量方式 |
|------|------|------|----------|
| 平均模块行数 | ~800 行 | <500 行 | `tokei` |
| 最大模块行数 | 3,439 行 | <1000 行 | `tokei` |
| 代码重复率 | ~15% | <5% | `cargo-detect-dupes` |
| 测试覆盖率 | ~65% | >85% | `cargo-tarpaulin` |
| 圈复杂度 | ~15 | <10 | `cargo-geiger` |

### 性能

| 指标 | 当前 | 目标 | 测量方式 |
|------|------|------|----------|
| 增量编译时间 | ~120s | <60s | `cargo build --timings` |
| 完整编译时间 | ~480s | <300s | `hyperfine` |
| 单元测试时间 | ~180s | <120s | `cargo test --timings` |
| 内存占用 | ~200MB | <150MB | `heaptrack` |
| 向量索引大小 | ~12MB | <5MB | 文件大小 |
| 记忆搜索延迟 | ~200ms | <80ms | 基准测试 |

### 架构

| 指标 | 当前 | 目标 | 验证方式 |
|------|------|------|----------|
| CLI 调用 Server API | ❌ 部分 | ✅ 100% | 代码审阅 |
| 模块职责清晰度 | ~40% | >80% | 架构审阅 |
| 接口一致性 | ~60% | >90% | API 审查 |
| 文档完整性 | ~60% | >90% | 文档覆盖率 |

---

## 📁 相关文档索引

### 架构设计

1. [SOLUTION.md](./SOLUTION.md) - v1.1.6 完整解决方案
2. [CODE_REUSE_STRUCTURE_DESIGN.md](./CODE_REUSE_STRUCTURE_DESIGN.md) - 代码复用结构设计
3. [MONOLITHIC_MODULES_ANALYSIS.md](./MONOLITHIC_MODULES_ANALYSIS.md) - 巨型模块分析
4. [CLI_GUIDE_OPTIMIZED.md](./CLI_GUIDE_OPTIMIZED.md) - CLI 使用指南优化

### 重构计划

5. [REFACTORING_EXECUTION_PLAN.md](./REFACTORING_EXECUTION_PLAN.md) - 8 周重构计划
6. [REFACTORING_SUMMARY.md](./REFACTORING_SUMMARY.md) - 重构总结
7. [REFACTORING_VISUAL.md](./REFACTORING_VISUAL.md) - 重构可视化

### 记忆优化

8. [MEMORY_ARCHITECTURE_DESIGN.md](./MEMORY_ARCHITECTURE_DESIGN.md) - 记忆架构设计
9. [MEMORY_INDEX_PRECISION_DESIGN.md](./MEMORY_INDEX_PRECISION_DESIGN.md) - 精准索引设计

### Phase 完成报告

10. [FINAL_SUMMARY.md](./FINAL_SUMMARY.md) - Phase 1-3 完成总结
11. [PHASE4-5_PROGRESS.md](./PHASE4-5_PROGRESS.md) - Phase 4-5 进度
12. [INTERIM_PROGRESS.md](./INTERIM_PROGRESS.md) - 中期进度

### Team 报告

13. [TEAM_A_IMPLEMENTATION_REPORT.md](./TEAM_A_IMPLEMENTATION_REPORT.md) - Team A 完成
14. [TEAM_B_SUMMARY.md](./TEAM_B_SUMMARY.md) - Team B 总结
15. [TEAM_D_QUICK_REFERENCE.md](./TEAM_D_QUICK_REFERENCE.md) - Team D 参考
... (共 17 个 Team 报告)

---

## 🚀 下一步行动

### 立即执行（今天）

1. **审阅本整合计划** - 确认任务拆分合理
2. **创建 Agent Pool** - 使用 `cis agent pool create`
3. **启动 Team V** - CLI 架构修复（P0 优先级）

### 本周执行

**Week 1 任务**:
- [ ] Team V: V-1 CLI 架构修复 (3-5 人日)
- [ ] Team Q: V-2 scheduler 设计 (2-3 人日)

### 预期成果

**Week 1 结束时**:
- ✅ CLI 架构修复完成（所有 handler 调用 Server API）
- ✅ scheduler 拆分方案确定
- ✅ Agent Pool 稳定运行

---

## 🎓 Agent Teams 最佳实践

### 1. 任务定义清晰

每个任务必须包含：
- ✅ 清晰的目标（可验证）
- ✅ 明确的验收标准
- ✅ 预估工作量（人日）
- ✅ 依赖关系
- ✅ 上下文数据（相关文档、代码）

### 2. Team 能力匹配

- ✅ 只分配 Team 能力范围内的任务
- ✅ 考虑 Team 负载（均衡分配）
- ✅ 优先依赖任务先完成

### 3. 事件驱动通信

- ✅ 任务分配事件 (`task.assigned`)
- ✅ 任务完成事件 (`task.completed`)
- ✅ 任务失败事件 (`task.failed`)
- ✅ Teams 订阅相关事件

### 4. 自动重试

- ✅ 失败任务自动重新分配
- ✅ 最多重试 3 次
- ✅ 3 次失败后标记为阻塞

### 5. 进度透明

- ✅ 实时任务状态
- ✅ Team 负载监控
- ✅ 预估完成时间
- ✅ 自动生成报告

---

**文档版本**: 1.0
**整合完成日期**: 2026-02-12
**作者**: CIS Architecture Team
**审核状态**: 待审核

**下一步**: 审阅本计划，准备启动 Agent Pool
