[package]
name = "cis-skill-init-wizard"
version = "1.1.3"
edition = "2021"
description = "Init Wizard Skill - 初始化引导，配置 AI 环境"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
