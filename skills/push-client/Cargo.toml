[package]
name = "cis-skill-push-client"
version = "0.1.0"
edition = "2021"
description = "Push Client Skill - 推送客户端（从 AgentFlow 迁移）"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
