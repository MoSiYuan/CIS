[package]
name = "cis-skill-ai-executor"
version = "0.1.0"
edition = "2021"
description = "AI Executor Skill - 抽象 AI Agent 执行层"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# 无 tokio/async，纯同步执行
